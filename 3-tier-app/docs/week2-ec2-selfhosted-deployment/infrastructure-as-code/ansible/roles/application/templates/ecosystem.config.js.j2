// PM2 Ecosystem Configuration for {{ app.name }}
// Generated by Ansible on {{ ansible_date_time.iso8601 }}

module.exports = {
  apps: [{
    name: '{{ pm2.app_name }}',
    script: 'server.js',
    cwd: '{{ app.app_dir }}/api',
    instances: {{ pm2.instances }},
    exec_mode: '{{ pm2.exec_mode }}',
    watch: {{ pm2.watch | lower }},
    max_memory_restart: '{{ pm2.max_memory_restart }}',
    env: {
      NODE_ENV: '{{ environment }}',
      PORT: {{ app.port }}
    },
    env_production: {
      NODE_ENV: 'production',
      PORT: {{ app.port }}
    },
    log_file: '{{ app.logs_dir }}/{{ pm2.app_name }}.log',
    out_file: '{{ app.logs_dir }}/{{ pm2.app_name }}-out.log',
    error_file: '{{ app.logs_dir }}/{{ pm2.app_name }}-error.log',
    log_date_format: 'YYYY-MM-DD HH:mm:ss Z',
    merge_logs: true,
    autorestart: true,
    max_restarts: 10,
    min_uptime: '10s',
    kill_timeout: 5000
  }]
};
